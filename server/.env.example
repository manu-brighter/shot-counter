PORT=5000
FRONTEND_ORIGIN=http://localhost:3000

# Optional — path to the SQLite file. Defaults to server/shot_counter.db.
# The desktop build overrides this to %APPDATA%\Shöttli-Counter\shot_counter.db.
# DB_PATH=./shot_counter.db
